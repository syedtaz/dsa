pub struct Solution {}

impl Solution {
  pub fn shuffle(nums: Vec<i32>, n: i32) -> Vec<i32> {
    let mut res = vec![0; nums.len()];
    for i in 0..(nums.len() / 2) {
      res[i * 2] = ;
    }
  }
}